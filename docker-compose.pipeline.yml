# docker-compose.pipeline.yml
# ─────────────────────────────────────────────────────────────────────────────
# VOD 광고 오버레이 파이프라인 — 전체 서비스 구성 (v2.14)
#
# 서버 배포 경로:
#   백엔드  : /app/Docker/pipeline/
#   프론트  : /app/Docker/frontend/
#   이 파일 : /app/Docker/pipeline/
#
# 이미지 구조 (서비스별 완전 분리):
#   vod-step1    : step1  (Dockerfile.step1)
#   vod-step2a   : step2-a (Dockerfile.step2a)
#   vod-step2b   : step2-b (Dockerfile.step2b)
#   vod-step2c   : step2-c (Dockerfile.step2c)
#   vod-step3    : step3  (Dockerfile.step3)
#   vod-step4    : step4  (Dockerfile.step4)
#   vod-step5    : step5-api (Dockerfile.step5)
#   vod-frontend : frontend (Dockerfile.frontend)
#
# 최초 실행:
#   cp .env.example .env  # .env 파일 생성 후 실제 값 입력
#   docker-compose -f docker-compose.pipeline.yml build
#   docker-compose -f docker-compose.pipeline.yml up -d
#
# 전체 중지:
#   docker-compose -f docker-compose.pipeline.yml down
#
# 로그 확인:
#   docker-compose -f docker-compose.pipeline.yml logs -f step2-a
# ─────────────────────────────────────────────────────────────────────────────

services:

  # ── Step 1: 전처리 (프레임 추출 / 오디오 디먹싱) ──────────────────────────
  step1:
    build:
      context: /app/Docker/pipeline/backend
      dockerfile: Dockerfile.step1
    image: vod-step1:latest
    restart: unless-stopped
    environment:
      DB_HOST: "${DB_HOST}"
      DB_PORT: "${DB_PORT:-5432}"
      DB_NAME: "${DB_NAME}"
      DB_USER: "${DB_USER}"
      DB_PASSWORD: "${DB_PASSWORD}"
      RABBITMQ_HOST: "${RABBITMQ_HOST}"
      RABBITMQ_PORT: "${RABBITMQ_PORT:-5672}"
      RABBITMQ_USER: "${RABBITMQ_USER}"
      RABBITMQ_PASSWORD: "${RABBITMQ_PASSWORD}"
      QUEUE_PREFIX: "${QUEUE_PREFIX:-prod}"
      STORAGE_BASE: "/app/storage"
      TZ: "${TZ:-Asia/Seoul}"
    volumes:
      - /app/HelloVision/data/vod:/vod:ro
      - /app/HelloVision/data/storage:/app/storage
      - /app/HelloVision/data/logs:/app/storage/logs

  # ── Step 2-A: 비전 분석 (YOLO + VLM 고정 샘플링) ─────────────────────────
  step2-a:
    build:
      context: /app/Docker/pipeline/backend
      dockerfile: Dockerfile.step2a
    image: vod-step2a:latest
    restart: unless-stopped
    environment:
      DB_HOST: "${DB_HOST}"
      DB_PORT: "${DB_PORT:-5432}"
      DB_NAME: "${DB_NAME}"
      DB_USER: "${DB_USER}"
      DB_PASSWORD: "${DB_PASSWORD}"
      RABBITMQ_HOST: "${RABBITMQ_HOST}"
      RABBITMQ_PORT: "${RABBITMQ_PORT:-5672}"
      RABBITMQ_USER: "${RABBITMQ_USER}"
      RABBITMQ_PASSWORD: "${RABBITMQ_PASSWORD}"
      QUEUE_PREFIX: "${QUEUE_PREFIX:-prod}"
      STORAGE_BASE: "/app/storage"
      VLM_BACKEND: "${VLM_BACKEND:-gemini}"
      GEMINI_API_KEY: "${GEMINI_API_KEY}"
      GEMINI_MODEL: "${GEMINI_MODEL:-gemini-2.0-flash}"
      TZ: "${TZ:-Asia/Seoul}"
      HF_HOME: "/models"
      TRANSFORMERS_CACHE: "/models"
    volumes:
      - /app/HelloVision/data/storage:/app/storage
      - /app/HelloVision/data/logs:/app/storage/logs
      - step2a_model_cache:/models

  # ── Step 2-B: 오디오 분석 (침묵 감지 + Whisper STT) ──────────────────────
  step2-b:
    build:
      context: /app/Docker/pipeline/backend
      dockerfile: Dockerfile.step2b
    image: vod-step2b:latest
    restart: unless-stopped
    environment:
      DB_HOST: "${DB_HOST}"
      DB_PORT: "${DB_PORT:-5432}"
      DB_NAME: "${DB_NAME}"
      DB_USER: "${DB_USER}"
      DB_PASSWORD: "${DB_PASSWORD}"
      RABBITMQ_HOST: "${RABBITMQ_HOST}"
      RABBITMQ_PORT: "${RABBITMQ_PORT:-5672}"
      RABBITMQ_USER: "${RABBITMQ_USER}"
      RABBITMQ_PASSWORD: "${RABBITMQ_PASSWORD}"
      QUEUE_PREFIX: "${QUEUE_PREFIX:-prod}"
      STORAGE_BASE: "/app/storage"
      WHISPER_MODEL: "${WHISPER_MODEL:-small}"
      TZ: "${TZ:-Asia/Seoul}"
      HF_HOME: "/models"
      TRANSFORMERS_CACHE: "/models"
    volumes:
      - /app/HelloVision/data/storage:/app/storage
      - /app/HelloVision/data/logs:/app/storage/logs
      - step2b_model_cache:/models

  # ── Step 2-C: Phase A (씬 분절 + VLM narrative, DB 플래그 게이트) ─────────
  step2-c:
    build:
      context: /app/Docker/pipeline/backend
      dockerfile: Dockerfile.step2c
    image: vod-step2c:latest
    restart: unless-stopped
    depends_on:
      - step2-a
      - step2-b
    environment:
      DB_HOST: "${DB_HOST}"
      DB_PORT: "${DB_PORT:-5432}"
      DB_NAME: "${DB_NAME}"
      DB_USER: "${DB_USER}"
      DB_PASSWORD: "${DB_PASSWORD}"
      RABBITMQ_HOST: "${RABBITMQ_HOST}"
      RABBITMQ_PORT: "${RABBITMQ_PORT:-5672}"
      RABBITMQ_USER: "${RABBITMQ_USER}"
      RABBITMQ_PASSWORD: "${RABBITMQ_PASSWORD}"
      QUEUE_PREFIX: "${QUEUE_PREFIX:-prod}"
      STORAGE_BASE: "/app/storage"
      VLM_BACKEND: "${VLM_BACKEND:-gemini}"
      GEMINI_API_KEY: "${GEMINI_API_KEY}"
      GEMINI_MODEL: "${GEMINI_MODEL:-gemini-2.0-flash}"
      TZ: "${TZ:-Asia/Seoul}"
      HF_HOME: "/models"
      TRANSFORMERS_CACHE: "/models"
    volumes:
      - /app/HelloVision/data/storage:/app/storage
      - /app/HelloVision/data/logs:/app/storage/logs
      - step2c_model_cache:/models

  # ── Step 3: 후보 페어 생성 (analysis_scene × ad_inventory) ────────────────
  step3:
    build:
      context: /app/Docker/pipeline/backend
      dockerfile: Dockerfile.step3
    image: vod-step3:latest
    restart: unless-stopped
    depends_on:
      - step1
    environment:
      DB_HOST: "${DB_HOST}"
      DB_PORT: "${DB_PORT:-5432}"
      DB_NAME: "${DB_NAME}"
      DB_USER: "${DB_USER}"
      DB_PASSWORD: "${DB_PASSWORD}"
      RABBITMQ_HOST: "${RABBITMQ_HOST}"
      RABBITMQ_PORT: "${RABBITMQ_PORT:-5672}"
      RABBITMQ_USER: "${RABBITMQ_USER}"
      RABBITMQ_PASSWORD: "${RABBITMQ_PASSWORD}"
      QUEUE_PREFIX: "${QUEUE_PREFIX:-prod}"
      STORAGE_BASE: "/app/storage"
      TZ: "${TZ:-Asia/Seoul}"
    volumes:
      - /app/HelloVision/data/storage:/app/storage
      - /app/HelloVision/data/logs:/app/storage/logs

  # ── Step 4: 스코어링 / 광고 결정 ─────────────────────────────────────────
  step4:
    build:
      context: /app/Docker/pipeline/backend
      dockerfile: Dockerfile.step4
    image: vod-step4:latest
    restart: unless-stopped
    depends_on:
      - step1
    environment:
      DB_HOST: "${DB_HOST}"
      DB_PORT: "${DB_PORT:-5432}"
      DB_NAME: "${DB_NAME}"
      DB_USER: "${DB_USER}"
      DB_PASSWORD: "${DB_PASSWORD}"
      RABBITMQ_HOST: "${RABBITMQ_HOST}"
      RABBITMQ_PORT: "${RABBITMQ_PORT:-5672}"
      RABBITMQ_USER: "${RABBITMQ_USER}"
      RABBITMQ_PASSWORD: "${RABBITMQ_PASSWORD}"
      QUEUE_PREFIX: "${QUEUE_PREFIX:-prod}"
      STORAGE_BASE: "/app/storage"
      TZ: "${TZ:-Asia/Seoul}"
      HF_HOME: "/models"
      TRANSFORMERS_CACHE: "/models"
    volumes:
      - /app/HelloVision/data/storage:/app/storage
      - /app/HelloVision/data/logs:/app/storage/logs
      - step4_model_cache:/models

  # ── Step 5: FastAPI 서버 ──────────────────────────────────────────────────
  step5-api:
    build:
      context: /app/Docker/pipeline/backend
      dockerfile: Dockerfile.step5
    image: vod-step5:latest
    restart: unless-stopped
    depends_on:
      - step1
    ports:
      - "8000:8000"
    environment:
      DB_HOST: "${DB_HOST}"
      DB_PORT: "${DB_PORT:-5432}"
      DB_NAME: "${DB_NAME}"
      DB_USER: "${DB_USER}"
      DB_PASSWORD: "${DB_PASSWORD}"
      RABBITMQ_HOST: "${RABBITMQ_HOST}"
      RABBITMQ_PORT: "${RABBITMQ_PORT:-5672}"
      RABBITMQ_USER: "${RABBITMQ_USER}"
      RABBITMQ_PASSWORD: "${RABBITMQ_PASSWORD}"
      QUEUE_PREFIX: "${QUEUE_PREFIX:-prod}"
      STORAGE_BASE: "/app/storage"
      AD_VIDEO_DIR: "/ads/video"
      AD_IMAGE_DIR: "/ads/banner"
      API_HOST: "0.0.0.0"
      API_PORT: "8000"
      API_BASE_URL: "${API_BASE_URL}"
      VOD_DIR: "/vod"
      TZ: "${TZ:-Asia/Seoul}"
    volumes:
      - /app/HelloVision/data/storage:/app/storage
      - /app/HelloVision/data/logs:/app/storage/logs
      - /app/HelloVision/data/ad_assets/video:/ads/video:ro
      - /app/HelloVision/data/ad_assets/image:/ads/banner:ro
      - /app/HelloVision/data/vod:/vod:ro

  # ── Frontend: Next.js ─────────────────────────────────────────────────────
  frontend:
    build:
      context: /app/Docker/pipeline/frontend
      dockerfile: Dockerfile.frontend
      args:
        BACKEND_URL: "http://step5-api:8000"
    image: vod-frontend:latest
    restart: unless-stopped
    depends_on:
      - step5-api
    ports:
      - "3000:3000"
    environment:
      BACKEND_URL: "http://step5-api:8000"
      TZ: "${TZ:-Asia/Seoul}"

# ── Named Volumes ─────────────────────────────────────────────────────────────
volumes:
  step2a_model_cache:
    driver: local
  step2b_model_cache:
    driver: local
  step2c_model_cache:
    driver: local
  step4_model_cache:
    driver: local
