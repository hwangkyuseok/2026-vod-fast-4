# requirements.step2b.txt
# step2-b 전용 의존성 — 침묵 감지 + Whisper STT
# (torch / torchvision 은 Dockerfile 에서 CPU-only 버전으로 별도 설치)

# ── 데이터베이스 / 메시지큐 ──────────────────────────────────────────────────
psycopg2-binary>=2.9.9
pika>=1.3.2

# ── 오디오 전사 (Whisper) ─────────────────────────────────────────────────────
openai-whisper>=20231117
librosa>=0.10.2
soundfile>=0.12.1

# ── 유틸리티 ─────────────────────────────────────────────────────────────────
numpy>=1.26.0
python-dotenv>=1.0.0
