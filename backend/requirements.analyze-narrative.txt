# requirements.analyze-narrative.txt
# analyze_ad_narrative.py 전용 최소 의존성
# (torch / torchvision 은 Dockerfile 에서 CPU-only 버전으로 별도 설치)

# ── 데이터베이스 ──────────────────────────────────────────────────────────────
psycopg2-binary>=2.9.9

# ── AI 모델 (Qwen2-VL) ────────────────────────────────────────────────────────
transformers>=4.44.0
accelerate>=0.33.0
qwen-vl-utils>=0.0.2

# ── 이미지 처리 ───────────────────────────────────────────────────────────────
Pillow>=10.4.0
numpy>=1.26.0

# ── 유틸리티 ──────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
